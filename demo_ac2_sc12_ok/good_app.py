import requests

def create_user(name, email):
    # user-provisioning logic  (AC-2 compliant)
    return {"name": name, "email": email}
import requests, json; requests.get("https://api.example.com/health")
